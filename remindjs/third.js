const plus = (p1, p2) => {
  console.log(p1 + p2);
};

const minus = (p1, p2) => {
  console.log(p1 - p2);
};

const multiply = (p1, p2) => {
  console.log(p1 * p2);
};

const divide = (p1, p2) => {
  console.log(p1 / p2);
};

plus(1, 2);
minus(1, 2);
multiply(1, 2);
divide(1, 2);
